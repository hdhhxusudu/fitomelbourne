{
  "name": "peptides",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-for-cloudflare.mjs",
    "dev": "next dev",
    "start": "next start",
    "lint": "eslint",
    "clean": "node scripts/clean-output.mjs",
    "cf:build": "node scripts/clean-output.mjs && npm run build",
    "preview": "npm run cf:build && opennextjs-cloudflare preview",
    "deploy": "npm run cf:build && wrangler deploy",
    "upload": "npm run cf:build && opennextjs-cloudflare upload",
    "deploy:dry-run": "npm run cf:build && wrangler deploy --dry-run",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts"
  },
  "dependencies": {
    "@opennextjs/cloudflare": "^1.19.11",
    "next": "16.2.7",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.7",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wrangler": "^4.98.0"
  }
}